// /* eslint-disable @typescript-eslint/no-explicit-any */
// export const saveTokenStorage = (result: any) => {
//     // Guarda el token en localStorage
//     localStorage.setItem('token', JSON.stringify(result));
//     console.log('Token guardado en localStorage:', result);
//     return result; // Devuelve el token que has pasado como argumento
// };
